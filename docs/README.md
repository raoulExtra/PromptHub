# Core requirements

The `CREQ` documents describe the core requirements of PromptHub and their
acceptance criteria. They cover prompt storage, criticality-aware prompt
assembly, the SQLite data model, Python extensions, Markdown transfer, and the
local GUI/API boundary.
